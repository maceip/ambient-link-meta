{
  "name": "face-chat-final-web",
  "version": "0.1.0",
  "private": true,
  "description": "Glasses-side SPA. Static; expects relay reachable at relative /face-chat/ws (front it with Caddy/nginx).",
  "scripts": {
    "serve": "python3 -m http.server 8080",
    "test": "node --test test/*.test.mjs"
  }
}
